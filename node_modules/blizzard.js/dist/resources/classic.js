'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const auctionHouse = (namespace, args) => {
    if (args.auctionHouse === undefined) {
        return {
            path: `data/wow/connected-realm/${encodeURIComponent(args.realm)}/auctions/index`,
            namespace,
        };
    }
    return {
        path: `data/wow/connected-realm/${encodeURIComponent(args.realm)}/auctions/${encodeURIComponent(args.auctionHouse)}`,
        namespace,
    };
};

exports.auctionHouse = auctionHouse;
