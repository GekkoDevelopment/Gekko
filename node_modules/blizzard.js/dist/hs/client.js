'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var core = require('../core/core.js');
var hs = require('../resources/hs.js');

class HS extends core.Blizzard {
    constructor() {
        super(...arguments);
        this.cardSearch = this.createClientResourceRequest(hs.cardSearch);
        this.card = this.createClientResourceRequest(hs.card);
        this.cardBacks = this.createClientResourceRequest(hs.cardBacks);
        this.deck = this.createClientResourceRequest(hs.deck);
        this.metadata = this.createClientResourceRequest(hs.metadata);
    }
}

exports.HS = HS;
